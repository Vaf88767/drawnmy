let ctx = document.createElement("")

class Actions {
    ctx : HTMLElement
    constructor(ctx:HTMLElement) {
      this.ctx = ctx
    }
    draw(i: string,x: number,y: number,width: number,height: number) {
        
    }
}

const createActions = new Actions(ctx)